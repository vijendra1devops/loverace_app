apiVersion: v1
kind: Service
metadata:
  name: core-api-svc
  namespace: loverace
spec:
  selector:
    app: core-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
